{
  "name": "redir-proweb",
  "version": "0.1.0",
  "description": "Protected site from a clonning and force redirect.",
  "main": "app.js",
  "scripts": {
    "lint": "standard --fix ./source/**/*.js || true"
  },
  "private": true,
  "author": "Fais",
  "keywords": ["redirect", "web", "forced", "load", "protect", "debug"],
  "homepage": "https://github.com/qya/cloudflare-redir_proweb",
  "bugs": "https://github.com/qya/cloudflare-redir_proweb/issues",
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "standard": "^9.0.2"
  },
  "standard": {
    "globals": [
      "INSTALL",
      "INSTALL_OPTIONS",
      "INSTALL_SCOPE",
      "INSTALL_ID"
    ]
  }
}
